// Package identity defines opaque aggregate identifiers for the Next domain.
//
// Aggregate identifiers use a deliberately narrow generated-ID syntax. Callers
// must allocate them independently of titles, product/model identifiers, audio
// identifiers, hashes, paths, and other mutable or external facts.
package identity
